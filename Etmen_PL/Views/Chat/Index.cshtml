@* =====================================================================
   View  : Chat/Index.cshtml
   Desc  : AI chat interface — message history list, message input box, send button, clear history button.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Index";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Chat.IndexViewModel
}

<h2>Chat — Index</h2>

@* TODO: Build the UI for: AI chat interface — message history list, message input box, send button, clear history button. *@
