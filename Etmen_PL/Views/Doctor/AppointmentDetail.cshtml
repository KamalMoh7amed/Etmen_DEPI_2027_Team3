@* =====================================================================
   View  : Doctor/AppointmentDetail.cshtml
   Desc  : Full appointment detail: patient info, slot time, notes, status change dropdown.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "AppointmentDetail";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.AppointmentDetailViewModel
}

<h2>Doctor — AppointmentDetail</h2>

@* TODO: Build the UI for: Full appointment detail: patient info, slot time, notes, status change dropdown. *@
