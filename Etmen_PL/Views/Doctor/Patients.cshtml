@* =====================================================================
   View  : Doctor/Patients.cshtml
   Desc  : Searchable patient list. 'Add Medical Record' button per row.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Patients";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.PatientsViewModel
}

<h2>Doctor — Patients</h2>

@* TODO: Build the UI for: Searchable patient list. 'Add Medical Record' button per row. *@
