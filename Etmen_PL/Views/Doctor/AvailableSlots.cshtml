@* =====================================================================
   View  : Doctor/AvailableSlots.cshtml
   Desc  : Calendar or table of availability slots. Add/delete slot controls.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "AvailableSlots";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.AvailableSlotsViewModel
}

<h2>Doctor — AvailableSlots</h2>

@* TODO: Build the UI for: Calendar or table of availability slots. Add/delete slot controls. *@
