@* =====================================================================
   View  : Doctor/Appointments.cshtml
   Desc  : Table of all doctor appointments with status and patient name. Link to detail.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Appointments";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.AppointmentsViewModel
}

<h2>Doctor — Appointments</h2>

@* TODO: Build the UI for: Table of all doctor appointments with status and patient name. Link to detail. *@
