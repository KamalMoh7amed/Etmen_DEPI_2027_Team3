@* =====================================================================
   View  : Doctor/Statistics.cshtml
   Desc  : Charts/tables: total appointments, completed, cancelled, monthly trends.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Statistics";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.StatisticsViewModel
}

<h2>Doctor — Statistics</h2>

@* TODO: Build the UI for: Charts/tables: total appointments, completed, cancelled, monthly trends. *@
