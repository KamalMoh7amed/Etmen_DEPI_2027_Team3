@* =====================================================================
   View  : Doctor/Profile.cshtml
   Desc  : Doctor profile edit form — Specialization, Bio, ConsultationFee, IsAvailable toggle.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Profile";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.ProfileViewModel
}

<h2>Doctor — Profile</h2>

@* TODO: Build the UI for: Doctor profile edit form — Specialization, Bio, ConsultationFee, IsAvailable toggle. *@
