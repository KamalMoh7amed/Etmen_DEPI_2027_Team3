@* =====================================================================
   View  : Doctor/Dashboard.cshtml
   Desc  : Doctor dashboard — today's appointments count, pending count, recent patients list.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Dashboard";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Doctor.DashboardViewModel
}

<h2>Doctor — Dashboard</h2>

@* TODO: Build the UI for: Doctor dashboard — today's appointments count, pending count, recent patients list. *@
