@* =====================================================================
   View  : Account/Register.cshtml
   Desc  : Registration form — Name, Email, Password, ConfirmPassword, Role selector.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Register";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Account.RegisterViewModel
}

<h2>Account — Register</h2>

@* TODO: Build the UI for: Registration form — Name, Email, Password, ConfirmPassword, Role selector. *@
