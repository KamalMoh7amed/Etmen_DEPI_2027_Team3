@* =====================================================================
   View  : Account/VerifyEmailNotice.cshtml
   Desc  : Info page telling user to check their email for verification link.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "VerifyEmailNotice";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Account.VerifyEmailNoticeViewModel
}

<h2>Account — VerifyEmailNotice</h2>

@* TODO: Build the UI for: Info page telling user to check their email for verification link. *@
