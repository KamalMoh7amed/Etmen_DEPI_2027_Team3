@* =====================================================================
   View  : Patient/MedicalRecords.cshtml
   Desc  : List of all medical records. Each row: date, bloodPressure, glucose, weight, BMI. 'Add Record' button opens inline form.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "MedicalRecords";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.MedicalRecordsViewModel
}

<h2>Patient — MedicalRecords</h2>

@* TODO: Build the UI for: List of all medical records. Each row: date, bloodPressure, glucose, weight, BMI. 'Add Record' button opens inline form. *@
