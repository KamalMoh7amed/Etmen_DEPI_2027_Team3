@* =====================================================================
   View  : Patient/Dashboard.cshtml
   Desc  : Patient Dashboard — shows upcoming appointments, latest risk level, unread alerts count, and recent medical records.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Dashboard";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.DashboardViewModel
}

<h2>Patient — Dashboard</h2>

@* TODO: Build the UI for: Patient Dashboard — shows upcoming appointments, latest risk level, unread alerts count, and recent medical records. *@
