@* =====================================================================
   View  : Patient/Appointments.cshtml
   Desc  : List of patient appointments with status badges. Cancel button for upcoming appointments.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Appointments";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.AppointmentsViewModel
}

<h2>Patient — Appointments</h2>

@* TODO: Build the UI for: List of patient appointments with status badges. Cancel button for upcoming appointments. *@
