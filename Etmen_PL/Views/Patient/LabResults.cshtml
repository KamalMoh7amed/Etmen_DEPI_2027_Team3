@* =====================================================================
   View  : Patient/LabResults.cshtml
   Desc  : Table of lab results: TestName, TestDate, Result, IsAbnormal. Upload lab result button.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "LabResults";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.LabResultsViewModel
}

<h2>Patient — LabResults</h2>

@* TODO: Build the UI for: Table of lab results: TestName, TestDate, Result, IsAbnormal. Upload lab result button. *@
