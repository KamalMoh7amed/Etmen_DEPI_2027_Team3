@* =====================================================================
   View  : Patient/RiskAssessment.cshtml
   Desc  : Risk assessment input form — fields from RiskInputDto: symptoms, vitals, lifestyle factors.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "RiskAssessment";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.RiskAssessmentViewModel
}

<h2>Patient — RiskAssessment</h2>

@* TODO: Build the UI for: Risk assessment input form — fields from RiskInputDto: symptoms, vitals, lifestyle factors. *@
