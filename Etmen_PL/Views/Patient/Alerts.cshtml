@* =====================================================================
   View  : Patient/Alerts.cshtml
   Desc  : List of alerts with read/unread styling. 'Mark all read' button.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Alerts";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.AlertsViewModel
}

<h2>Patient — Alerts</h2>

@* TODO: Build the UI for: List of alerts with read/unread styling. 'Mark all read' button. *@
