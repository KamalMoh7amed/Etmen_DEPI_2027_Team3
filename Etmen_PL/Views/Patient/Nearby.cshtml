@* =====================================================================
   View  : Patient/Nearby.cshtml
   Desc  : Map or list of nearby healthcare providers. Search form with location/radius/type filters.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Nearby";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.NearbyViewModel
}

<h2>Patient — Nearby</h2>

@* TODO: Build the UI for: Map or list of nearby healthcare providers. Search form with location/radius/type filters. *@
