@* =====================================================================
   View  : Patient/Profile.cshtml
   Desc  : Patient profile edit form — fields: FirstName, LastName, DateOfBirth, Gender, Phone, Address, BloodType, Allergies.

   TODO  : Implement this view. The controller passes the appropriate DTO
           as the model. Use the existing _Layout.cshtml for the shell.

   Model : Set @model at the top once the controller action is implemented.
   ===================================================================== *@

@{
    ViewData["Title"] = "Profile";
    // TODO: Set the correct @model directive, e.g.:
    // @model Etmen_BLL.DTOs.Patient.ProfileViewModel
}

<h2>Patient — Profile</h2>

@* TODO: Build the UI for: Patient profile edit form — fields: FirstName, LastName, DateOfBirth, Gender, Phone, Address, BloodType, Allergies. *@
